name: Build & Release

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python 3.12
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install build deps
        run: |
          pip install -r requirements.txt

      - name: Build AppImage / binary
        run: pyinstaller --onefile app.py

      - name: Archive build
        run: |
          mkdir -p dist
          cp dist/app dist/ProtonDeskX.v${{ github.run_number }}.x86_64.AppImage

      - name: Upload artifact (for download)
        uses: actions/upload-artifact@v4
        with:
          name: ProtonDeskX
          path: dist/ProtonDeskX.v${{ github.run_number }}.x86_64.AppImage

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3

      - name: Download built binary
        uses: actions/download-artifact@v4
        with:
          name: ProtonDeskX

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ github.run_number }}
          name: Release ${{ github.run_number }}
          files: |
            ProtonDeskX.v${{ github.run_number }}.x86_64.AppImage
          draft: false
          prerelease: false
